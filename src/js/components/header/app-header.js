/** @jsx React.DOM */

var React = require('react');

var Header = React.createClass({
  render: function() {
    return (
        <div>
          <h4>Im the Header</h4>
        </div>
    );
  }
});

module.exports = Header;